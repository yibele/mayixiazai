
  <t-navbar title="视频下载" left-arrow fixed bind:left-click="goBack" />
<!-- miniprogram/pages/download/download.wxml -->
<view class="page-container">

  <!-- 视频信息展示 -->
  <view class="video-info-card">
    <video 
      class="video-player" 
      src="{{videoInfo.downloadUrl}}" 
      poster="{{videoInfo.cover}}"
      show-play-btn="{{true}}"
      show-center-play-btn="{{true}}"
      controls="{{true}}"
      object-fit="contain"
    ></video>
    <view class="video-details">
      <view class="video-title">{{videoInfo.title}}</view>
    </view>
  </view>

  <!-- 功能按钮区域 -->
  <view class="action-section">
    <!-- 主要操作按钮 - 横向完整宽度 -->
    <t-button 
      class="action-button main-btn" 
      theme="primary" 
      block 
      bind:tap="downloadVideo"
    >
      <t-icon name="download" slot="icon"/>
      下载视频到相册
    </t-button>

    <t-button 
      class="action-button main-btn" 
      theme="default" 
      block 
      bind:tap="copyVideoUrl"
    >
      <t-icon name="link" slot="icon"/>
      复制无水印链接
    </t-button>

    <!-- 次要操作按钮 - 两个并排 -->
    <view class="secondary-buttons">
      <t-button 
        class="action-button secondary-btn" 
        theme="default" 
        bind:tap="copyCoverUrl"
      >
        <t-icon name="image" slot="icon"/>
        复制封面
      </t-button>

      <t-button 
        class="action-button secondary-btn" 
        theme="default" 
        bind:tap="copyTitle"
      >
        <t-icon name="file-copy" slot="icon"/>
        复制标题
      </t-button>
    </view>
  </view>

  <!-- 温馨提示 -->
  <view class="tips-section">
    <t-divider content="温馨提示" />
    <view class="tips-content">
      <view class="tip-item">
        <t-icon name="info-circle" size="16px" />
        <text>如果视频无法直接下载到相册，说明该视频域名不在小程序白名单中</text>
      </view>
      <view class="tip-item">
        <t-icon name="bulletpoint" size="16px" />
        <text>您可以点击"复制无水印链接"，然后在浏览器中打开进行下载</text>
      </view>
      <view class="tip-item">
        <t-icon name="heart" size="16px" />
        <text>我们会持续完善白名单，为您提供更好的下载体验</text>
      </view>
    </view>
  </view>

  <!-- 页脚 -->
  <t-footer
    class="page-footer"
    text="本服务仅供个人学习与收藏，请勿用于商业用途。"
  ></t-footer>
</view>